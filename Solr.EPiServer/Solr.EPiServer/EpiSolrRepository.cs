using EPiServer.ServiceLocation;
using Solr.Client;

namespace Solr.EPiServer
{
    //[ServiceConfiguration(ServiceType = typeof(ISolrRepository), Lifecycle = ServiceInstanceScope.HttpContext)]
    //public class EpiSolrRepository : SolrRepository
    //{
    //}
}
